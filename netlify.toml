# Netlify configuration - simple static site deploy
[build]
  publish = "public"
  command = "echo 'Deploying pre-built static files'"

# Handle SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
